# E2E 视觉测试报告 — Roguelike Auto-Battler

**日期:** 2026-06-08
**工具:** Playwright MCP（浏览器自动化）驱动 `window.__PHASER_GAME__`，逐场景截图人工走查。
**构建:** dev server `http://localhost:3000/roguelike/`，v1.16.0，canvas 800×450。
**方法:** 由于 Phaser 全部渲染到单一 `<canvas>`（无 DOM 子节点），通过 `browser_evaluate` 调用 `game.scene.start(...)` / 场景方法驱动，`browser_take_screenshot` 截图后人工判读。

## 场景走查结果（10 + 设置）

| # | 场景 | 进入方式 | 判定 | 备注 |
|---|------|---------|------|------|
| 1 | MainMenu | 直接加载 | ✅ | 6-7 按钮布局整齐、无溢出；运行中存档时正确显示"继续"按钮 |
| 2 | HeroDraft | `HeroDraftScene` | ✅ | 5 解锁英雄 chibi + 属性正常；**锁定格解锁提示为小字(9-10px)且相邻略重叠**（可读性，P2）|
| 3 | Map | `startRun()` | ✅ | 节点连线可见、节点类型图标正常、英雄面板 + 阵型按钮齐全；首图教程提示正常触发 |
| 4 | Battle | `selectNode(0)` | ✅ | 战斗正常结算（3 英雄造成 87/138/195 伤害、3 击杀），单位/伤害管线工作 |
| 5 | Reward/Victory | 战斗胜利 | ✅ | 胜利结算屏：金币/经验/存活/逐英雄战斗统计/总金币，布局清晰 |
| 6 | Shop | `ShopScene` | ✅ | 金币、英雄页签、协同预览、4 商品卡(名/属性/价/购买)齐全；首商店教程触发 |
| 7 | Event | `EventScene` | ✅ | **随机抽中 `dragon_egg`（本次新接入的孤儿事件之一）并正常渲染** — P1-4 修复实地验证 ✓ |
| 8 | Rest | `RestScene` | ✅ | 休息/训练/搜索 三选项、颜色区分、队伍 HP 状态正常 |
| 9 | Help | `showHelpPanel()` | ✅ | 不透明背景(可读)、元素克制/反应/羁绊/机制分区；确认龙之威严仍为 2 人(P1-6 故意保留) |
| 10 | Codex | `showCodexPanel()` | ✅ | 英雄/怪物页签、已解锁 5/26、英雄网格 + chibi、不透明背景 |
| — | Settings | `SettingsScene` | ✅ | **新增"文字大小"无障碍开关并实地验证不溢出**（压缩存档行间距换得空间，净零位移）|

## Phase 4 — 视觉问题修复（已落地）

- **主菜单版本号陈旧** `src/i18n.ts:23` — 显示 `v1.11.0`，实际 `package.json` 为 `1.16.0`。已改为 `v1.16.0`，浏览器复验显示正确。

## 顺带实地确认（本会话代码改动）

- **孤儿事件接入生效**：地图生成 55 节点（18 战斗/18 事件/8 商店/3 gauntlet/3 休息/3 boss/2 精英）无报错；事件场景实际抽中并渲染了 `dragon_egg`（修复前永不可达）。
- **教程系统正常**：首图/首商店/首事件提示均按预期触发（印证 06-07 P0-2 修复在运行时确实生效）。
- **文字缩放无障碍**：底层(`textScale`)+ 设置页开关(默认 100%)均渲染正常、布局不溢出。

## 残留（非阻塞，记录）

- **HeroDraft 锁定格解锁提示小字密排/轻微重叠**（P2 可读性）— 属抽卡格布局固有密度；玩家可用新增的文字缩放缓解，但默认仍偏小。若要根治需调整抽卡格栅格布局。
- **弹窗 backdrop 透明度不统一**（06-07 ux P2）— 本次走查的 Help/Codex 用高不透明度(可读)，未触及低透明度(0.4)的明细弹窗(如 CodexDetailPopup)。保留为打磨项。
- **基础战斗金币偏高**（首战 +26 vs 文档 12-18）— 印证 06-07 P1-2 经济虚胖；与利息一并留作平衡联调(见主报告推迟项)。

## 结论

10 个核心场景 + 设置页全部通过视觉走查，**无 P0/P1 视觉缺陷**。本会话代码改动在真实运行环境中表现正常，且 `dragon_egg` 孤儿事件接入获得实地验证。Phase 4 修复 1 个 P2（版本号），并把 06-08 主报告推迟的"文字缩放设置页 UI"补完并实地验证。
